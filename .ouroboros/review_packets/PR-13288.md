# PR 13288 — fix(pool): use time.monotonic() instead of time.time() for connection timestamps

- **Upstream PR:** #13288
- **Related issues:** #13169
- **Subsystem:** connection pool
- **Shape:** one source commit
- **Dependencies:** none
- **Files:** 1
- **Focused validation:** not applicable — no changed runnable pytest target was identified
- **Apply:** `git cherry-pick 91feee81b9b077bf41f47b8f757120e293f767f6`
- **Queue:** Needs design or branch decision

## Problem and intended behavior

Replaced all `time.time()` calls in `pool/base.py` with `time.monotonic()` for connection record timestamp tracking. Also updated the comment block that documented the Windows precision caveat. `time.time()` on Windows has approximately 16 ms granularity, which can cause soft invalidation checks to fail when `_soft_invalidate_time` and `starttime` are captured in the same timer quantum. `time.monotonic()` is monotonic by definition and provides sub-millisecond precision on all platforms, so the `>` comparisons between timestamps work correctly regardless of platform timer resolution. Four sites changed (all in `lib/sqlalchemy/pool/base.py`): - `Pool._invalidate_time` assignment (line 407) - `_ConnectionRecord._soft_invalidate_time` assignment (line 812) - Connection recycle time check in `get_connection` (line 838) - `_ConnectionRecord.

## Scope and provenance

- Suggested target: Maintainer choice; this unit has no changelog fragment
- Original contributor: `algojogacor`
- Included commit author or authors: Arya Rizky
- Frozen base: `aa1a5575358d3aa14953b04dced02f4763fed2e7`

Selected delta or maintained evidence files:

- `lib/sqlalchemy/pool/base.py`


## Gerrit-shaped review

- Suggested topic if selected: `option-pr-13288`
- Cross-packet dependencies: none
- Exact-file overlaps with other packets: none
- Upstream implementation feedback recorded: Yes. Read the upstream discussion and resolve it before upload.

Commit order:

1. `91feee81b9b077bf41f47b8f757120e293f767f6` — fix(pool): use time.monotonic() instead of time.time() for connection timestamps

## Focused validation

**Result:** not applicable — no changed runnable pytest target was identified

No focused pytest command applies to this packet.

Receipt-backed totals: 0 collected, 0 passed, 0 skipped, 0 failed, 0 errors.

## Additional test and changelog evidence

Changed test files:

No test file is isolated in this unit.

Changelog fragments:

No changelog fragment is isolated in this unit.

Combined branch evidence: 25800 passing and 1502 skipped non-typing tests, with zero integration-only failures. Typing has 77 passing tests and one stored-baseline failure.

## Apply

### Recommended

```bash
git cherry-pick 91feee81b9b077bf41f47b8f757120e293f767f6
```

Inspect:

```bash
git diff --stat 59aac193f48e67dc2d4070033f50d34503b644e4^1 59aac193f48e67dc2d4070033f50d34503b644e4
```


### Integrated-branch reproduction

This secondary command reproduces the delta as it appears in the combined integration branch. The original commit command above is the normal review path.

```bash
git cherry-pick -m 1 59aac193f48e67dc2d4070033f50d34503b644e4
```


## Decision required

Resolve the recorded upstream implementation feedback before electing this unit for Gerrit upload.
